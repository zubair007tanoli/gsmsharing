document.addEventListener('DOMContentLoaded', function () { // Toggle password visibility const togglePasswordButtons = document.querySelectorAll('.toggle-password'); togglePasswordButtons.forEach(button => { button.addEventListener('click', function() { const input = this.parentElement.querySelector('input'); const icon = this.querySelector('i'); if (input.type === 'password') { input.type = 'text'; icon.classList.remove('fa-eye'); icon.classList.add('fa-eye-slash'); } else { input.type = 'password'; icon.classList.remove('fa-eye-slash'); icon.classList.add('fa-eye'); } }); }); // Voting functionality const voteButtons = document.querySelectorAll('.vote-up, .vote-down'); voteButtons.forEach(button => { button.addEventListener('click', function() { const voteCount = this.parentElement.querySelector('.vote-count'); let count = parseInt(voteCount.textContent); if (this.classList.contains('vote-up')) { count++; this.style.color = 'var(--success-color)'; this.parentElement.querySelector('.vote-down').style.color = 'var(--text-lighter)'; } else { count--; this.style.color = 'var(--error-color)'; this.parentElement.querySelector('.vote-up').style.color = 'var(--text-lighter)'; } voteCount.textContent = count; }); }); // Mobile menu toggle const createMobileMenu = () => { const header = document.querySelector('header'); const nav = document.querySelector('header nav'); if (!header || !nav) return; const mobileMenuButton = document.createElement('button'); mobileMenuButton.classList.add('mobile-menu-toggle'); mobileMenuButton.innerHTML = ''; header.insertBefore(mobileMenuButton, nav); mobileMenuButton.addEventListener('click', function() { nav.classList.toggle('mobile-nav-active'); if (nav.classList.contains('mobile-nav-active')) { this.innerHTML = ''; } else { this.innerHTML = ''; } }); }; // Check if we need to create a mobile menu if (window.innerWidth <= 768) { createMobileMenu(); } window.addEventListener('resize', function() { if (window.innerWidth <= 768 && !document.querySelector('.mobile-menu-toggle')) { createMobileMenu(); } }); // Form validation const authForm = document.querySelector('.auth-form'); if (authForm) { authForm.addEventListener('submit', function(e) { e.preventDefault(); const email = document.getElementById('email'); const password = document.getElementById('password'); let isValid = true; if (!email.value || !email.value.includes('@')) { email.style.borderColor = 'var(--error-color)'; isValid = false; } else { email.style.borderColor = 'var(--border-color)'; } if (!password.value || password.value.length < 6) { password.style.borderColor = 'var(--error-color)'; isValid = false; } else { password.style.borderColor = 'var(--border-color)'; } if (isValid) { // Simulate login success alert('Login successful! Redirecting...'); window.location.href = 'index.html'; } }); } // Reply form functionality const replyForm = document.querySelector('.post-reply-form form'); if (replyForm) { replyForm.addEventListener('submit', function(e) { e.preventDefault(); const textarea = this.querySelector('textarea'); if (!textarea.value.trim()) { textarea.style.borderColor = 'var(--error-color)'; return; } // Simulate reply submission alert('Your reply has been posted!'); textarea.value = ''; textarea.style.borderColor = 'var(--border-color)'; }); } // Format buttons functionality const formatButtons = document.querySelectorAll('.format-btn'); const replyTextarea = document.querySelector('.post-reply-form textarea'); if (formatButtons.length && replyTextarea) { formatButtons.forEach(button => { button.addEventListener('click', function() { const format = this.title.toLowerCase(); const textarea = replyTextarea; const start = textarea.selectionStart; const end = textarea.selectionEnd; const selectedText = textarea.value.substring(start, end); let formattedText = ''; switch (format) { case 'bold': formattedText = `**${selectedText}**`; break; case 'italic': formattedText = `*${selectedText}*`; break; case 'link': formattedText = `[${selectedText}](url)`; break; case 'code': formattedText = `\`${selectedText}\``; break; case 'list': formattedText = `\n- ${selectedText}`; break; case 'image': formattedText = `![${selectedText}](image-url)`; break; default: formattedText = selectedText; } textarea.value = textarea.value.substring(0, start) + formattedText + textarea.value.substring(end); textarea.focus(); textarea.selectionStart = start + formattedText.length; textarea.selectionEnd = start + formattedText.length; }); }); } // Load more functionality const loadMoreButtons = document.querySelectorAll('.load-more-btn'); loadMoreButtons.forEach(button => { button.addEventListener('click', function() { // Simulate loading more content this.innerHTML = ' Loading...'; setTimeout(() => { this.innerHTML = 'Load More'; alert('No more content to load.'); }, 1500); }); }); });